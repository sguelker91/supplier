/**
 * DI-Token für `DeliveryAuthorizationRepository`
 * (siehe `delivery-authorization-repository.interface.ts`), analog
 * `CONTRACT_REPOSITORY`.
 */
export const DELIVERY_AUTHORIZATION_REPOSITORY = Symbol('DELIVERY_AUTHORIZATION_REPOSITORY');
